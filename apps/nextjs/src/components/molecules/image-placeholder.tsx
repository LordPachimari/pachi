import { Image } from "lucide-react";

const ImagePlaceholder = () => {
  return (
    <div className="bg-grey-5 flex h-full w-full items-center justify-center rounded-soft">
      <Image size={16} />
    </div>
  );
};

export default ImagePlaceholder;
