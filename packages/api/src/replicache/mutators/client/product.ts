export type ProductsMutators = typeof productsMutators;
export const productsMutators = {
  //the reason publish method is in product rep and not dashboard
  //so that the mutator triggers pull specifically in the product rep
};
